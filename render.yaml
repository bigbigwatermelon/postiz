services:
  # Redis 服务
  - type: redis
    name: postiz-redis
    plan: free
    ipAllowList: []  # 允许所有 IP 连接

  # 后端服务
  - type: web
    name: postiz-backend
    env: node
    plan: free
    buildCommand: |
      npm install -g pnpm@10.6.1 &&
      pnpm install &&
      pnpm run prisma-generate &&
      pnpm run build:backend
    startCommand: pnpm run start:prod:backend
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
      - key: DATABASE_URL
        fromDatabase:
          name: postiz-database
          property: connectionString
      - key: REDIS_URL
        fromService:
          name: postiz-redis
          type: redis
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: FRONTEND_URL
        value: https://postiz-frontend.onrender.com
      - key: NEXT_PUBLIC_BACKEND_URL
        value: https://postiz-backend.onrender.com
      - key: BACKEND_INTERNAL_URL
        value: http://localhost:3000
      - key: STORAGE_PROVIDER
        value: local
      - key: IS_GENERAL
        value: "true"
      - key: API_LIMIT
        value: "30"

  # 前端服务  
  - type: web
    name: postiz-frontend
    env: node
    plan: free
    buildCommand: |
      npm install -g pnpm@10.6.1 &&
      pnpm install &&
      pnpm run build:frontend
    startCommand: pnpm run start:prod:frontend
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 4200
      - key: NEXT_PUBLIC_BACKEND_URL
        value: https://postiz-backend.onrender.com



databases:
  - name: postiz-database
    databaseName: postiz
    user: postiz_user
    plan: free 